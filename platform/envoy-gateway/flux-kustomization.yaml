
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: envoy-gateway
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./platform/envoy-gateway
  prune: true
  sourceRef:
    kind: ExternalArtifact
    name: cluster
  dependsOn:
    - name: cert-manager   # 👈 reference by metadata.name of the other Kustomization
